Design a simple key-value store with append-only semantics, idempotent writes,
and a single-process invariant. The store must support reads at any time and
must not lose data on power loss. Out of scope: replication, sharding,
encryption.
